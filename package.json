{
  "name": "tracker",
  "version": "0.1.0",
  "description": "Simple daily tracker — buttons only, backed by Notion.",
  "private": true,
  "scripts": {
    "dev": "cd frontend && python3 -m http.server 8080"
  }
}
